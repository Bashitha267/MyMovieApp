import React from 'react';
import { StyleSheet, View } from 'react-native';

const User = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default User;
